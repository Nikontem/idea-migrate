# SDD ledger — plan: ~/Projects/idea-migrate/docs/superpowers/plans/2026-08-29-idea-migrate.md

Spec: docs/superpowers/specs/2026-08-29-jetbrains-project-relocation-design.md (read)
Branch: build-idea-migrate
MERGE_BASE: 11278ba3c2c71758154c91a924559a422425ee75

## Pre-flight conflict scan

Interface pairs (producer -> consumer), checked one row per shared file/interface:

| Producer | Consumer | Interface checked | Finding |
|---|---|---|---|
| T1 errors.py | T4,T6,T8,T9,T11,T13 | PathValidationError, XmlIntegrityError, BackupError, IdeRunningError, UndoError, MigrateError | all six defined in T1; names match consumers |
| T1 __version__ | T7,T13 | manifest tool_version, --version flag | matches |
| T1 Config | T13 | backup_root, jetbrains_root, exclude_products | matches |
| T2 find_product_dirs(root, exclude) | T13 | call passes (config.jetbrains_root, config.exclude_products) | matches |
| T3 running_ides(ps_output) | T9 | assert_no_ide_running delegates | matches |
| T4 MoveSpec(source,dest,home,same_device) | T9,T13 | field access spec.same_device/source/dest/home | matches |
| T5 prompt_for_path(prompt) | T13 | returns Path | matches |
| T6 prefix_variants/rewrite_products/count_references | T13 | arg order and return types | matches |
| T7 Manifest (10 fields) | T10,T11,T13 | T13 constructs all 10; T10/T11 read subsets | matches |
| T8 new_backup_dir/back_up_products/write_undo_script | T13 | signatures | matches |
| T9 assert_no_ide_running/move_directory | T11,T13 | signatures | matches |
| T10 find_backups/format_backups | T13 | signatures | matches |
| T11 undo_backup(backup_dir, jetbrains_root, now, ps_output=None) | T13 | T13 calls with 3 positional args | matches (ps_output defaults) |
| T12 format_plan/format_result | T13 | arg order | matches |
| T13 build_parser/run_migration | T14 | round-trip test imports both | matches |

File ownership: every task creates distinct files; no task modifies a file another task created. No conflict.

Self-consistency per task: tests specified match the code specified in each task.
Task 4 was corrected during plan self-review (unimplementable "source inside destination"
rule removed, replaced with case-insensitive same-directory check; expected count 9 -> 10).

Findings and rulings:

Ruling: Task 13's test helper `MigrationTestCase._args(**overrides)` carries unused
keyword-override machinery that cannot work (argparse flags contain dashes, which are not
valid Python keyword names). Implementer is instructed to build argv inline instead.
Why: dead, non-functional test code would be flagged by review anyway.
Cost if wrong: none - it is unused in every call site in the plan.

## Progress
Task 1: implemented (commit e0d31dd, 6/6 tests pass). Plan said 5 tests; test file has 6 methods. Harmless plan miscount.
Ruling: review of task N runs concurrently with implementation of task N+1 whenever task N+1
imports nothing the task N review could invalidate. Why: 15 sequential dispatch+review round
trips is the dominant cost here, and the independence is checkable from the preflight interface
table. Cost if wrong: at most one task's rework if a review forces a signature change that a
concurrently-running task consumed - mitigated by only pairing genuinely independent tasks.
Task 1: complete (commits 11278ba..e0d31dd, review clean - spec OK, quality approved)
Task 1: minor (deferred): load_config silently falls back to defaults when the config path exists but is not a regular file - no diagnostic.
Task 1: minor (deferred): test_config_is_frozen asserts broad Exception rather than dataclasses.FrozenInstanceError.
Task 1: minor (deferred): no test covers overriding jetbrains_root (backup_root and exclude_products are covered).
Task 2: implemented (commit 36bfb15, 5/5 tests pass)
Task 2: complete (commits e0d31dd..36bfb15, review clean - spec OK, quality approved, no Critical/Important)
Task 2: minor (deferred): products.py resolve() on candidates could rename a symlinked product dir inconsistently with the pre-filter name check. Not realistic for JetBrains config dirs.
Task 3: implemented (commit 30d2d5e, 6/6 tests pass)
Task 3: complete (commits 36bfb15..30d2d5e, review clean - spec OK, quality approved, no Critical/Important)
Task 3: minor (deferred): matching is by bare executable basename, so an unrelated binary literally
named idea/rider/clion outside an .app bundle would register as that IDE. Plan-mandated design.
Ruling: leave it, and do NOT add a --skip-ide-check override. Why: the false positive is very
unlikely (requires a non-bundled binary with exactly an IDE's name) and its only cost is a spurious
refusal the user can clear by quitting the process; an override flag would let anyone bypass the one
check preventing silent config corruption, which is a far worse failure. Cost if wrong: a user hits
a spurious block and has to rename or quit a process to proceed.
Task 4: implemented (commit ab93f0e, 10/10 tests, full suite 27) - DONE_WITH_CONCERNS
Ruling: Task 4 concern 1 is a real defect in MY plan text and is fixed before review, not parked.
str(src).lower() == str(dst).lower() is a string comparison standing in for filesystem identity.
On a case-SENSITIVE APFS volume it wrongly rejects two genuinely different directories as "the same"
(a false positive blocking a legitimate move), and it misses ".." spellings. Replacement:
dst.exists() and _same_dir(src, dst) - if the destination does not exist it cannot be the same
directory, and if it does exist samefile gives filesystem truth on both volume types.
Cost if wrong: none identified; strictly more correct than the string compare.
Ruling: Task 4 concern 2 (check ordering) folded into the same fix - move the destination-inside-
source check above the parent-exists check so the more specific message wins. Cost if wrong: none,
both orderings refuse the move; only the message differs.
Ruling: Task 4 concern 3 (paths made absolute but not resolved, so symlink spelling is preserved)
is deliberate and stays. Why: the IDEs recorded whatever spelling the user opened the project
under, and the rewrite matches on that stored text - resolving symlinks here would produce a path
that does not match what is in the config files. Cost if wrong: a project opened through a symlink
whose config stores the resolved path would not be repaired; the tool reports surviving references
rather than failing silently.
Task 4: fix round 1/5 (3 addressed, 0 open; commits ab93f0e..55ad76a; 11 tests, suite 28)
Task 5: implemented (commit 5c7ae6e, 9/9 tests, suite 37)
Task 4: review found 1 Critical, 3 Important, 4 Minor (all reproduced by the reviewer, not speculative).
Ruling: the Critical (_is_inside walks lexical parents, so a symlinked destination parent pointing
into the source escapes the containment check) enters the fix loop. Why: this is the single check
that exists to stop a directory being moved into itself; today only the kernel's EINVAL stops it,
and that backstop vanishes on Task 9's cross-device copy-then-delete branch, where it becomes a
recursive copy into itself. Cost if wrong: none - realpath seeding is strictly more correct.
Ruling: the vacuous tilde test is plan-mandated (my brief specified it) and the reviewer is right.
It passes two already-absolute strings and asserts they are absolute, so expanduser has zero
coverage while the spec requires "~" to work at the prompt. Overriding my own brief here.
Cost if wrong: none; the replacement sets HOME to the temp root and stays inside the no-real-home rule.
Ruling: minors folded into this same dispatch (not a separate round) where they are safety-relevant:
weak substring assertion that would pass for the wrong reason, untested not-writable branch,
same_device using stat instead of lstat. Deferred: the redundant dst.exists() guard (documents
intent) and the home-does-not-exist no-op (harmless while the CLI passes a real home).
Task 5: complete (commits 55ad76a..5c7ae6e, review clean - spec OK, quality approved, no Critical/Important)
Task 5: minor (deferred): path_candidates' "no directory part at all" branch (typing "Webst" with no slash, falls back to cwd) is correct but untested.
Task 5: minor (deferred): prompt_for_path cannot request only_dirs=False; deliberate, keeps the Task 13 signature exact.
Task 5: minor (deferred): test_state_iterates_then_returns_none checks the iterate-then-terminate contract but not candidate contents.
Task 4: fix round 2/5 (7 addressed, 0 open; commit 67e6e50; 15 tests, suite 58)
Task 6: implemented (commit cc5a993) - notification pending
Ruling (correcting my earlier concurrency ruling): a FIX ROUND is an implementer, so it must never
run concurrently with another task's implementer. My earlier ruling only excluded parallel
implement(N)/implement(N+1) and I let Task 4's fix round overlap Task 6's implementation. Task 6
observed Task 4's uncommitted changes in its working tree mid-task. No damage - Task 6 used explicit
paths in git add and committed only its own files, and Task 4's fix committed cleanly afterwards as
67e6e50. From here: only reviewers run concurrently with an implementer or fix round, never two
writers. Cost if wrong: none; this is strictly more conservative.
Task 6: implemented (commit cc5a993, 17/17 tests, suite 58) - DONE_WITH_CONCERNS
Ruling: Task 6 concerns 1 and 2 are real and enter a fix round rather than being parked. Why: this is
the module that can corrupt the user's IDE config, and concern 2 is precisely the silent-miss failure
the design warns about - a path at the end of an XML text node is not matched, so the tool would
report success while leaving a broken reference. Concern 1 (file:// variants can double-apply when the
destination nests under the source) is unreachable today only because validate_move rejects that case
in a DIFFERENT module; a corruption guard that depends on a caller elsewhere is not a guard.
Cost if wrong: a single-pass alternation is marginally harder to read than four sequential passes.
Ruling: Task 6 concern 3 (brief said 18 tests, code has 17) is my prose miscount, not a defect. No action.
Task 6: fix round 1/5 (2 addressed, 0 open; commit bbefa3c; 19 tests, suite 60)
Task 4: re-review of fix round 2 - all 7 findings ADDRESSED (reviewer verified empirically by running
the new tests against the pre-fix paths.py; exactly the 3 claimed tests fail there, for the right reasons).
Task 4: NEW breakage introduced by the fix diff (Important) - accepting ".." destinations is correct,
but Path.absolute() does not collapse "..", so MoveSpec.dest carries the literal spelling and Task 13's
prefix_variants would write "$USER_HOME$/WebstormProjects/../Projects/WebstormProjects" into the IDEs'
config files. Functional for the OS, garbage for anything reading it back. Joins the open findings ->
fix round 3. Queued behind Task 7 because a fix round is a writer and only one writer runs at a time.
Task 4: minor (deferred): report claimed the false _is_inside docstring summary line was replaced; only
the paragraph beneath it was. The line is now true on the merits, but the report overstated the change.
Task 4: minor (deferred): two blank lines between methods inside TestValidMove where PEP 8 wants one. No linter configured.
Task 7: implemented (commit 5ab2c96, 4/4 tests)
Task 4: fix round 3/5 (1 addressed, 0 open; commit 5266e9d; 15 tests, suite 64)
Task 7: review found 1 Important (non-atomic manifest write), 2 Minor.
Ruling: the Important enters a fix round. Why: manifest.json is the single source of truth for undo
and the backups listing; write_text writes in place, so a crash mid-write leaves a truncated file and
read_manifest then raises an unhandled JSONDecodeError the next time the user lists backups or tries
to undo - i.e. the safety net fails exactly when it is needed. rewrite.py already uses temp-file +
os.replace, so this is also a consistency fix. Cost if wrong: three extra lines in a tiny module.
Task 7: minor (deferred): frozen dataclass holds list/dict fields, so hash(manifest) raises TypeError
(auto __hash__ over a tuple containing a list). Immaterial today; CARRY FORWARD as a note to Tasks 10
and 11 so neither puts a Manifest in a set or uses one as a dict key.
Task 7: minor (deferred): MANIFEST_VERSION is defined but never compared against data["version"], so
schema versioning is aspirational; Manifest(**data) raises a bare TypeError on drift. YAGNI-acceptable,
no forward-compat requirement in the spec.
Task 6: review found 1 Critical, 3 Important, 4 Minor. The Critical is MY amendment's fault.
Ruling: my fix-round-1 amendment widened _BOUNDARY to include \s on the reasoning that whitespace
"cannot appear inside a path component being extended". That reasoning is WRONG for the space
character - space is legal in macOS directory names. Reviewer reproduced: with old=~/Projects,
"/Users/tester/Projects 2024/x" is rewritten to "/Users/tester/Moved 2024/x". So my amendment
reintroduced exactly the sibling-corruption that boundary anchoring exists to prevent, and made it
WORSE than the original brief. Correct boundary: [/"'<>\r\n\t] - explicit CR/LF/tab keeps the
text-node case the amendment was written for, without matching space. Cost if wrong: none; strictly
narrower than \s and the text-node test still passes.
Ruling: the "skip malformed file with a warning" conflict (brief prose requires a warning, brief code
emits nothing) is resolved with stdlib logging in rewrite.py rather than a changed return signature.
Why: a returned skip list would ripple into Task 13's brief and the manifest schema; a logger call
does not. Task 13 must call logging.basicConfig so warnings reach stderr - carried forward.
Cost if wrong: warnings are less prominent than report lines; mitigated by the CLI's final report
already printing surviving-reference counts.
Task 6: minor (deferred): parent directory is never fsynced after os.replace (durability, not
corruption - content is never torn). Not worth the complexity for config files.
Task 4: complete (commits 30d2d5e..5266e9d across 3 fix rounds, re-review clean, no new breakage, 15 tests)
Task 7: fix round 1/5 (1 addressed, 0 open; commit d81cec9; suite 65)
Task 7: re-review - finding ADDRESSED (atomic write mechanics correct: temp file in the same
directory, flush, fsync, close, then os.replace; no EXDEV risk), but the covering test is weak.
Ruling: accept the code fix and close Task 7 rather than spending a round on the test. The reviewer
is right that test_overwrite_leaves_complete_file_and_no_leftover_temp_files would pass against the
OLD write_text implementation, so it does not cover the property it names. But the property matters
enough to test somewhere, so instead of a fourth serialized fix round on a tiny module, the coverage
is being ADDED TO TASK 14 (the round-trip safety test, Opus): patch os.replace to raise mid-write and
assert the previous manifest.json survives intact rather than being truncated. Task 14 is the natural
home for recovery-property tests and is not yet dispatched, so this costs nothing.
Cost if wrong: if Task 14 somehow drops it, the atomicity property ships correct but untested.
Task 7: minor (deferred): temp file is not removed if os.replace itself raises. Inherited from the
rewrite.py pattern the brief told it to match; same gap exists there and is being fixed in Task 6
fix round 2, so rewrite.py and manifest.py will diverge - flag to the final review for consistency.
Task 7: complete (commits bbefa3c..d81cec9, fix round 1, re-review ADDRESSED, suite 65)
Task 6: fix round 2/5 (7 addressed, 0 open; commit 06b2c41; 25 tests, suite 71)
Task 8: implemented (commit c4cfe74, 9/9 tests, suite 80)
Task 6: re-review of fix round 2 - all 7 ADDRESSED, no new breakage, PASS. Reviewer independently
verified the named-group backtracking behaviour and that lastgroup can never be None.
Ruling: the residual on ">" enters fix round 3 rather than being parked, even though the reviewer
called it a deferred nit. I put ">" in the prescription myself and it is wrong for an asymmetric
reason the reviewer worked out: XML REQUIRES "<" to be escaped, so a raw "<" after a path can only be
markup and never a filename byte - but XML does NOT require ">" to be escaped, and ">" is legal in a
macOS filename. Reviewer reproduced corruption of a directory named "Projects>2024" sitting beside a
moved "Projects". Crucially it buys nothing: no legitimate XML construct puts ">" immediately after a
path (comments end "--", CDATA "]]", PIs "?"), so it is an inert character whose only effect is to be
a corruption vector. Same class of bug as the space, just rarer. Cost if wrong: none - strictly
narrower, and the reviewer confirmed no covered case depends on it.
Task 6: minor (deferred): a path followed by a space or vertical tab before its closing tag is not
rewritten. Accepted cost of excluding the space from the boundary - missing beats corrupting, and the
run reports surviving references so a miss is visible.
Task 9: implemented (commit 79acbc1, 3/3 tests)
Task 8: review found 2 Important, 3 Minor, no Critical.
Ruling: Important 1 (undo.sh has ZERO behavioural test coverage - only executability, bash -n, a
substring match, and the no-manifest refusal) enters a fix round. This is a defect in MY brief, not
the implementer's work. It matters more than any other test gap in the project: the user's stated
requirement for this whole tool was "so we are sure that if something breaks we have a way to
restore", and undo.sh is the recovery path of last resort - the one that must work when the Python
tool is what broke. Shipping it with no test that it actually reverses a move is indefensible.
Cost if wrong: the behavioural tests need a stub pgrep on PATH to avoid a spurious refusal when the
developer has an IDE open; if that stubbing is fragile the tests could be flaky.
Ruling: Important 2 (test_script_accepts_a_path_argument asserts the literal substring '${1:-' in the
script source rather than that passing a path actually changes which directory is operated on) is
folded into the same round - a broken implementation behind that substring would still pass.
Task 8: minor (deferred): new_backup_dir's collision loop is TOCTOU-racy across processes; two
concurrent runs could both pass exists() and the second mkdir raises an uncaught FileExistsError.
Fails safe (crashes rather than overwriting), so robustness not data safety.
Task 8: minor (deferred): undo.sh's pgrep guard is a broad substring match that can false-positive on
an unrelated process whose command line mentions JetBrains. Biases toward refusing rather than
destroying, which is the correct failure direction.
Task 9: review found 1 Critical, 3 Important, 1 Minor. The Critical is MY brief's design, not the
implementer's transcription.
Ruling: the Critical enters a fix round. My brief verified the cross-device copy with dest.is_dir().
shutil.copytree creates the destination root BEFORE copying any children, so that check passes for a
tree that is arbitrarily incomplete - and the very next statement is shutil.rmtree(source). A disk-full
or permission failure that leaves a partial copy would therefore delete the only good copy of the
user's projects. This is the single most destructive defect found in the build. Replacement: walk both
trees and compare relative path sets plus per-file sizes using lstat (NOT stat - symlinks must be
compared as symlinks, not followed), and only remove the source when they agree.
Cost if wrong: comparing sizes is not a checksum, so a same-size corrupted copy would pass; full
hashing of an 11 GB tree is disproportionate, and the branch only runs across filesystems.
Ruling: symlinks=True STAYS, against the reviewer's concern. A move must preserve symlinks as symlinks -
that is what rename does, and following them would duplicate data and change semantics. The reviewer's
worry that rmtree could delete a link's target is overstated: a link pointing inside the source has its
target copied too, and a link pointing outside is untouched by rmtree. Cost if wrong: a relative
symlink pointing outside the moved tree can break, same as with mv.
Ruling: the bare OSError on verification failure gets a new MoveError(MigrateError) in errors.py.
Touching Task 1's file is acceptable - purely additive - and the CLI's contract is that every
deliberate failure is a MigrateError it can print without a traceback.
Task 6: fix round 3/5 (1 addressed, 0 open; commit 4d1a891; 26 tests, suite 84)
Task 6: complete (commits 5c7ae6e..4d1a891 across 3 fix rounds, re-review ADDRESSED, 26 tests, suite 84)
Task 9: fix round 1/5 (4 addressed, 0 open; commit 2d348d3; 5 tests, suite 86). Fault-injection test
confirmed to fail against the old dest.is_dir() code.
Ruling (from user question): the tool already supports migrating a SINGLE project rather than a whole
IDE root - --source/--dest are arbitrary directories and prefix matching is anchored, so
"--source ~/WebstormProjects/tokenizer --dest ~/Projects/_trial/tokenizer" moves one project and
rewrites only its references. No code change needed. But trialling directly into
~/Projects/WebstormProjects/ would make that directory exist and then block the whole-root move with
"destination already exists". REQUIRED README CONTENT for Task 15: document the trial workflow -
migrate one project into a throwaway destination, verify in the IDE, then undo it from its backup,
which exercises move + repair + restore on real data and leaves a clean starting point.
Task 8: fix round 1/5 (3 addressed, 0 open; commit 0b53272; 15 tests inc 7 behavioural undo.sh, suite 92)
Task 9: complete (commits c4cfe74..2d348d3, fix round 1, re-review ADDRESSED, no path to data loss found)
Task 9: minor (deferred): the snapshot compares file SIZE, not content, so a same-length corruption
during a cross-device copy would go undetected and the source would still be deleted. Ruling: accept.
Hashing an 11 GB tree is disproportionate and no realistic copy failure produces a same-length
corruption. FOR THE FINAL REVIEW FIX WAVE: add one sentence to the move_directory docstring stating
that verification is by path/kind/size and not by content hash, so the limit is documented rather
than implied.
Task 9: minor (deferred): a comment in the fault-injection test wrongly claims shutil.copytree
recurses via a re-lookup of shutil.copytree; on CPython 3.11+ it recurses through private _copytree,
so the inner mock.patch.object is unnecessary. Harmless, comment is wrong.
Task 8: complete (commits 06b2c41..0b53272, fix round 1, re-review pass, 15 tests, suite 92)
Task 10: implemented (commit 202cfdf, 7/7 tests, suite 99)
Task 11: implemented (commit 7535a91, 7/7 tests)
Task 10: review found 2 Important (both plan-mandated, i.e. my brief), 3 Minor, no Critical.
Ruling: both Important findings enter a fix round. (a) The undone-backup test asserts the "rolled back"
label appears but never asserts the "undo:" line is ABSENT - so the suite proves the label changes,
not the suppression, which is the property that actually matters. (b) find_backups swallows
JSONDecodeError (a ValueError) and TypeError from Manifest(**data), so a backup with a CORRUPT manifest
vanishes from the listing with no diagnostic - and that is precisely the backup a user most needs to
find. Fix: keep skipping it, but log a warning naming the directory, consistent with the logging
approach already ruled for rewrite.py. Cost if wrong: a warning line for a directory the user
deliberately parked in the backup root.
Ruling: the two cheap Minors fold into the same round - the unreachable trailing return in _human_size,
and the hardcoded "undo.sh" literal which should import UNDO_SCRIPT_NAME from backup.py so a rename
cannot silently desync the displayed path.
Task 10: minor (deferred): newest-first string sort inverts at 10+ same-second collisions ("-10" sorts
before "-9"). Requires ten runs inside one second; not worth the complexity.
Task 11: review found no Critical. Guard ordering verified correct (manifest check -> already-undone
check -> IDE check -> only then any mutation; no input reaches destructive work before all three pass).
Ruling: mark_undone being called only at the end is CORRECT and stays. The reviewer traced the partial
failure case: if the config restore raises midway, undone_at stays None, and a retry hits the
"source already exists" branch which declines rather than re-moving, while the config copytree is
idempotent. So leaving the stamp unset correctly signals "not fully undone, safe to retry" instead of
falsely claiming success. No change.
Ruling: copytree(dirs_exist_ok=True) merge semantics STAY, against the reviewer's alternative of
rmtree-then-copy for a byte-exact restore. Why: the tool's whole philosophy is never to delete what it
did not create. An exact restore would silently discard any settings the user changed between the
migration and the undo - and someone who migrated, worked for a week, then undid would lose that week.
The cost is that the restored config is a mix of pre-migration files and anything created since, which
can leave a stale recent-projects entry pointing at the vacated path. IDEs handle a missing recent
entry gracefully. Cost if wrong: a cosmetic stale entry the user can remove from the welcome screen.
Ruling: the untested decline branches (source already exists / destination missing) enter a small fix
round. These are the "declines rather than destroys" property, undo.sh already has equivalent tests
from Task 8, and the Python route should not be the weaker of the two.
Task 10: fix round 1/5 (4 addressed, 0 open; commit 00c96a9; 10 tests, suite 109)
Task 11: fix round 1/5 (1 addressed + docstring; commit 8426857; 9 tests, suite 111)
Task 10: complete (commits 0b53272..00c96a9, fix round 1, re-review clean, 10 tests, suite 109)
Task 11: complete (commits 202cfdf..8426857, fix round 1, re-review clean, 9 tests, suite 111)
Task 12: implemented (commit 6cd1a05, 8/8 tests)
Task 12: complete (commits 8426857..6cd1a05, review clean - spec OK, quality approved, no Critical/Important)
Task 12: reviewer verified the last-line invariant STRUCTURALLY, not just by test: the remaining-references
and hardcoded-warnings blocks both append before the final extend, which is the last statement before
the return, so no path can emit anything after the undo command. rstrip().splitlines()[-1] lands on the
undo line, not a blank.
Task 12: minor (deferred) FOR THE FINAL REVIEW FIX WAVE: the zero case reads "Repaired 0 path references
in 0 files." with no cue whether that means nothing needed fixing or the tool failed to find references
at all. Genuinely ambiguous user-facing text and worth one clearer sentence.
Task 12: minor (deferred): the "->" arrow lines are indented identically in format_plan and format_result
but sit under labels of different width ("Move:" vs "Moved:"), so they do not align. Cosmetic, inherited
from my brief verbatim.
Task 12: minor (deferred): test_backup_path_appears_near_the_end largely duplicates the last-line test,
since the last line itself contains the backup path.
Task 13: implemented (commit 15e18b0, 9/9 tests, suite 128, no signature drift)
Task 14: implementer agent TERMINATED by an API error (machine slept mid-response). It had written
tests/test_roundtrip.py (untracked, 7 tests) but had not committed. File verified by the controller:
all 7 pass, exit 0, including the round trip and the rerouted manifest-atomicity tests.
Task 14: the agent independently found the divergence this ledger predicted after Task 7's review -
rewrite.py unlinks its temp file when os.replace fails, manifest.py does not, so a failed replace
leaks a temp file into the backup directory forever. It recorded this as an expectedFailure with a
full explanation rather than deleting the test or silently passing.
Ruling: fix manifest.py rather than ship the expectedFailure. Why: the leak inflates the size shown by
"idea-migrate backups" and leaves dead weight in a directory the tool promises never to clean up, and
the fix is three lines already proven in rewrite.py. Shipping a known-gap marker in the recovery path
when the fix is trivial is the wrong trade. This authorizes a src/ change from Task 14, overriding its
brief's "tests only" rule. Cost if wrong: none identified; the pattern is already reviewed in rewrite.py.
Ruling: re-dispatch as a FRESH implementer (the original agent is dead), in two commits - the tests
as written first, then the manifest.py fix plus dropping the expectedFailure marker - so the review
sees the gap and its fix as separate, legible changes.
Task 13: review found 1 Critical, 5 Important, 4 Minor. Ordering traced CLEAN - backup and undo.sh
complete and the manifest lands before anything moves, so there is no window where the directory has
moved without a manifest recording it, and a crash between the two manifest writes still leaves a
recoverable backup.
Ruling: the Critical enters a fix round. If anything raises AFTER move_directory succeeds - and
XmlIntegrityError from rewrite_products is reachable, e.g. a destination path containing "&" or "<" -
the user gets one error line and exit 1, and is NEVER TOLD a backup exists or where undo.sh is.
backup_dir is bound at that point and simply not printed. The state is recoverable, but the user has
no way to know that. This defeats the tool's entire purpose at exactly the moment it matters.
Ruling: all five Important findings join the same round. Three are real crash paths (raw OSError from
mkdir/write/rename escaping as a traceback; _find_hardcoded_paths' unguarded rglob crashing AFTER a
successful migration and destroying the success report; KeyboardInterrupt/EOFError). Two are tests that
do not test what they claim - the dry-run test still passes if rewrite.py's dry_run guard is deleted,
and all nine tests still pass if the second write_manifest call is removed entirely.
Ruling: cheap Minors folded in - dataclasses.replace for the second manifest write instead of retyping
ten fields, the wrong _find_hardcoded_paths docstring, and rejecting --dry-run with subcommands
(today "idea-migrate --dry-run undo /path" performs a REAL undo while the flag says it writes nothing).
Task 13: minor (deferred): move_status is write-only - neither undo_backup nor undo.sh reads it, since
recovery works off filesystem state. Harmless but implies a guarantee that does not exist.
Task 13: fix round 1/5 (9 addressed, 0 open; commit 59c261f; 11 tests, suite 137)
Task 13: re-review of fix round 1 - 8 of 9 ADDRESSED (verified by mutation: the reviewer deleted each
guard and confirmed the strengthened test fails). 1 partial, 1 NEW gap. Both enter fix round 2.
Ruling: the NEW gap is the same class as the Critical just fixed and is MORE likely in practice. The
recovery route is keyed to the EXCEPTION TYPE (except MigrateError/OSError) rather than to whether the
move happened. So Ctrl-C after the move - the worst possible timing, including mid-copytree/rmtree on
the cross-device path where the tree is genuinely half-migrated - escapes run_migration entirely and
prints only "Cancelled." with exit 130. The backup exists; the user is never told. Any non-OSError bug
post-move loses it to a traceback the same way. Fix: drive the recovery print from the `moved` flag
via except BaseException / finally, so the message follows the FILESYSTEM STATE rather than the
exception class. Cost if wrong: the recovery banner could print on an exception where the move did not
actually happen - guarded by the moved flag, so it cannot.
Ruling: the EOFError finding is only half fixed and the rest enters the same round. EOFError is caught
at the "Proceed?" prompt but prompt_for_path in completion.py still calls bare input(), so
"idea-migrate < /dev/null" with no --source ends in a traceback. Confirmed by the reviewer's injection.
Task 13: reviewer confirmed no masking risk from the widened except - no MigrateError subclass inherits
OSError, and rewrite.py's own OSError handling still runs first.
Task 14: implemented (commits 8c74dae test + bc0eed0 manifest fix; 137 pass, NO expected failures left).
The fresh agent found a real weak assertion in the inherited file: test_all_products_are_repaired
checked only options/recentProjects.xml, never the workspace/AAA.xml the fixture creates holding the
old path - it confirmed by mutation that a regression skipping workspace/ entirely passed all seven
tests silently. Now asserts both directories for all three products, and that the OLD path is gone
rather than only that the new one is present. It also hardened the snapshot helper to record permission
bits and handle symlinks explicitly (entries that were neither file nor dir were being dropped from the
digest, a hole in a comparison claiming to cover everything).
Task 14: honest limitation recorded by the agent - the round trip always takes the same-device
os.rename path, so it never exercises the cross-device copy branch, and never the documented merge
behaviour where undo leaves IDE-created files in place. Task 9 has its own cross-device tests.
Task 13: fix round 2/5 (2 addressed, 0 open; commit 91c2f36; 14 tests, suite 140)
Task 14: complete (commits 59c261f..bc0eed0, review approved, 4 Minor, manifest fix confirmed to match
rewrite.py structurally). Reviewer verified every claim by mutation, and DISPROVED the implementer's own
caveat: chmod-ing restored config to 0644 does make the round trip fail, so the permission field bites.
It also proved the round trip is the SOLE guard on undo's workspace/ half - mutating undo.py to restore
only options/ fails test_migrate_then_undo_restores_everything and nothing else in the suite.
Ruling: the untested undo-merge contract is REQUIRED WORK FOR THE FINAL REVIEW FIX WAVE, not a new task
fix round. The documented behaviour - a file the IDE created after migration survives an undo rather
than being deleted - is covered nowhere in the whole suite. That is a decision I made deliberately
(never delete what the tool did not create) against the reviewer's wipe-and-replace alternative, and
nothing currently stops someone "fixing" it back. One small test in test_undo.py pins it. Batching it
into the final fix wave rather than a 16th dispatch. Cost if wrong: if the final wave drops it, a
deliberate design decision ships unpinned.
Task 14: minor (deferred) FOR THE FINAL FIX WAVE: assert the 0o600 fixture file's mode BETWEEN migrate
and undo, not only after undo - removing os.chmod from rewrite.py currently passes the whole suite
because undo's copytree restores the mode anyway.
Task 14: minor (deferred): the Downloads/gamma anti-corruption assertion is inert - it shares no prefix
with the source under any plausible regression. Do not count it as coverage.
Task 14: minor (deferred): only an os.replace failure is simulated for manifest atomicity; a failure
inside handle.write/fsync is covered structurally by the same try but never exercised.
Task 14: NOT a gap - the round trip never executes undo.sh, but Task 8 has 7 behavioural tests that run
it via subprocess, so the standalone recovery route is covered there.
Task 13: complete (commits 6cd1a05..91c2f36 across 2 fix rounds, re-review ADDRESSED, 14 tests, suite 140).
Reviewer confirmed moved=True is set immediately after move_directory returns and never earlier, so a
failure DURING the move correctly withholds a recovery message for a move that never happened; and that
Python dispatches to only the first matching except arm, so no double-print path exists.
Ruling: REQUIRED WORK FOR THE FINAL REVIEW FIX WAVE - my prescription told prompt_for_path to catch
KeyboardInterrupt alongside EOFError. Consequence: Ctrl-C at "Source directory:" now prints
"No input available - pass --source and --dest explicitly" and exits 1, instead of a clean cancel at
130. Genuine interactive cancellation is being reported as a non-interactive failure, on the most
common way a user aborts a prompt. Fix: convert only EOFError; let KeyboardInterrupt propagate to
main()'s existing handler. Cost if wrong: none - strictly narrower catch, and main() already handles it.

=== REQUIRED FINAL-WAVE FIX LIST (hand to the final whole-branch review) ===
1. completion.py: prompt_for_path must convert only EOFError, not KeyboardInterrupt (above).
2. test_undo.py: pin the documented undo-merge contract - a file the IDE created after migration
   survives an undo. Covered nowhere in the suite; it is a deliberate design decision with nothing
   stopping someone reverting it.
3. mover.py: one sentence in the move_directory docstring stating cross-device verification is by
   path/kind/size, not content hash.
4. report.py: the zero case reads "Repaired 0 path references in 0 files" with no cue whether nothing
   needed fixing or nothing was found.
5. test_roundtrip.py: assert the 0o600 fixture mode BETWEEN migrate and undo, not only after.
Task 15: implemented (commit ae51b16, --help and backups verified clean, no doc/code discrepancy)
Task 15: complete (commits 91c2f36..ae51b16, review approved, 3 Minor, every factual claim verified
against the code including the never-deletes-backups promise and the trial-project trap).
FINAL-WAVE FIX LIST additions from the Task 15 review:
6. README: document --yes and --version, and describe the interactive "Proceed? [y/N]" confirmation -
   a reader currently cannot tell that a migration pauses by default or how to skip it.
7. README Configuration: the key list reads as exhaustive but omits jetbrains_root.
8. README opening: say WHY a plain Finder move breaks things - the IDEs store each project's absolute
   path outside the project itself. Currently inferable from later sections but never stated.
=== FINAL WHOLE-BRANCH REVIEW: NOT FIT TO RUN. 2 Critical, 5 Important. One fix wave dispatched. ===
Ruling: promote the Task 8 deferred minor (undo.sh's broad pgrep) to CRITICAL. The reviewer ran the
script's exact pattern against the live process table and it MATCHES JetBrains Toolbox (pid 1374) and
the JetBrains daemon (pid 1942), both of which autostart on this machine. So undo.sh exits 1 before
restoring anything - and all three places the tool tells a user how to recover point ONLY at undo.sh.
The recovery route the user is shown is the one that does not work. My earlier ruling that this "biases
toward refusing rather than destroying, the correct failure direction" was wrong in the way that
matters: refusing to recover is not a safe failure when it is the only recovery route offered.
Ruling: Critical 1 (rewrite.py drops the $USER_HOME$ variant pair when the destination is outside the
home directory, so a move to an external volume silently repairs nothing AND reports success, because
count_references uses the same variant list) is fixed even though it does not affect the planned
migration. "Move my projects to the external drive" is a normal thing to want and the failure mode is a
confident false success.
Ruling: the silent multi-minute pause is added to the fix wave as progress output. Not cosmetic - the
reviewer notes _find_hardcoded_paths walks the entire moved tree AFTER the riskiest step, so the user
is most tempted to hit Ctrl-C at exactly the wrong moment.
FINAL FIX WAVE: complete, 4 commits 6a612eb / 0796b3e / cd7acfc / af1feb4. Suite 140 -> 157 (17 added).
Both C1 tests confirmed failing against pre-fix rewrite.py. C2 verified against the LIVE process table:
old pattern matched 7 processes including Toolbox (1374) and jetbrainsd (1942); new pattern matches
nothing. A generated undo.sh was then RUN END TO END against a throwaway home in /tmp with Toolbox and
the daemon running, and completed - the exact case that was broken. The pgrep test stub was rewritten
to honour the pattern it is given rather than return a fixed exit code, since a stub that ignores the
pattern cannot distinguish an IDE from Toolbox.
Note for the re-review: the XML preflight means run_migration now calls rewrite_products TWICE, so the
three "failure after the move" tests use a two-element side_effect (preflight succeeds, second raises).
FIX WAVE RE-REVIEW: everything ADDRESSED except half of C2 - and the remaining half is a defect MY
prescription introduced. I told the implementer to narrow undo.sh's guard to
pgrep -f '/(idea|pycharm|...)$'. But pgrep -f matches the ENTIRE COMMAND LINE INCLUDING ARGUMENTS, so
the "$" anchor means the guard only fires when the IDE process has no arguments. Reviewer's evidence
from this machine: jetbrainsd runs as ".../jetbrainsd run" and is not matched; and the Toolbox launcher
on PATH runs IntelliJ as 'open -na ".../idea" $wait --args ...', so "idea ~/Projects/foo" from a
terminal produces exactly the command line the guard misses.
Ruling: this is WORSE than the bug it replaced and gets one more targeted fix. The original bug made
undo.sh refuse to run - annoying but safe. This one makes it restore settings while IntelliJ is running,
after which the IDE flushes its in-memory settings over them on quit, SILENTLY DISCARDING THE ROLLBACK -
on the one code path that exists for when things have already gone wrong. Fix: mirror processes.py
exactly by using "ps -Ao comm= | grep -Eqi ..." instead of pgrep -f. comm= strips arguments, which is
why the same "$"-anchored pattern is correct there and wrong under pgrep -f.
Ruling: also fixing the two non-blockers THIS WAVE introduced - "idea-migrate undo" passing
config.jetbrains_root while undo.sh now reads the manifest's (a new divergence), and the XML preflight
sitting after new_backup_dir so a rejected destination leaves an orphan "pending" backup in the listing.
Not fixing the hypothetical KeyError on a manifest missing "home" - no version ever wrote one.
Targeted fix: commit b2ec9c7, suite 157 -> 159.
My prescribed "if ps -Ao comm= | grep -Eqi ...; then" was ALSO wrong and the implementer refused it,
with a measurement. set -e is suspended in an if condition but PIPEFAIL IS NOT. grep -q exits on first
match while ps is still writing (48,897 bytes against macOS's 16 KB pipe buffer), so ps dies of SIGPIPE
and pipefail reports the pipeline as 141. Measured PIPESTATUS: 141 0, and the if printed DOES NOT FIRE
on a pattern that MATCHED - the same guard inversion in a different disguise. The script now captures
the output and tests the string, with || true for the no-match case.
Item 2 needed one change beyond the prescription: to let the caller prefer the recorded jetbrains_root
and fall back to config, the manifest must be able to say "not recorded". Manifest.jetbrains_root is now
str | None = None and read_manifest no longer fills it with a guess indistinguishable from a real value.
Side effect: the data["home"] KeyError I ruled not-fixing no longer exists - the code is gone.
FIT TO RUN — final verification passed
